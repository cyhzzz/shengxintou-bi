# =====================================================================
# 省心投 BI - 后端依赖
#
# Python 版本要求：3.9+（实测 3.13 兼容）
# 安装方式（二选一）：
#   1. 一键脚本（推荐，会自动创建 .venv 并完成全部依赖安装）：
#        bash scripts/setup.sh          # Unix / macOS / Git Bash / WSL
#        scripts\setup.bat              # Windows
#        python scripts/setup.py        # 任何平台（兜底入口）
#   2. 手动安装：
#        python -m venv .venv
#        .venv\Scripts\pip install -r requirements.txt
# =====================================================================

Flask==3.1.3
flask-cors==6.0.5
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
SQLAlchemy==2.0.51
python-dotenv==1.2.2
pandas==3.0.3
numpy==2.5.1
openpyxl==3.1.5
requests==2.34.2
webdavclient3==3.14.7
lxml==6.1.1

# feat-desktop-supabase：Supabase PostgreSQL + Auth 依赖
psycopg[binary]>=3.2,<4
supabase>=2.7,<3

# feat-local-auth 方案 A：本地 JWT 鉴权（不依赖 Supabase Auth API）
PyJWT>=2.8,<3
werkzeug>=3.0,<4

# 可选：Swagger/OpenAPI 文档（app.py 已对缺失做兼容，装了才有 /apidocs）
# flasgger