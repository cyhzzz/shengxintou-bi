# 开发环境配置

# API 基础路径（开发环境留空走 Vite 代理 /api -> 127.0.0.1:5000，避免 CORS）
# 注意：直连模式需后端 CORS 白名单包含 3000 端口，当前白名单只有 5173/5000，勿改回直连
VITE_API_BASE_URL=

# 应用标题
VITE_APP_TITLE=省心投 BI

# 环境
VITE_ENV=development